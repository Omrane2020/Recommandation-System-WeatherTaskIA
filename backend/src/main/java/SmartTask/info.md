src/main/java/com/smartactivity
│── config/          -> Spring Security, JWT filters
│── controller/      -> Endpoints REST
│── service/         -> Logique métier
│── model/           -> Entités (User, Preferences…)
│── repository/      -> Repos JPA
│── dto/             -> Objets envoyés au frontend
│── utils/           -> JWT utils
